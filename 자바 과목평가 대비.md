
## 객체지향 / 객체 참조 / 메모리 할당

### 메모리 할당

```java
public class Person {
    static final String NATION = "KOREA";   // ① static final 상수
    static int count = 0;                    // ② static 변수
    
    String name;                             // ③ 인스턴스 변수 (참조형)
    int age;                                 // ④ 인스턴스 변수 (기본형)
    int[] scores;                            // ⑤ 인스턴스 변수 (배열 참조)

    public Person(String name, int age) {    // ⑥ 매개변수
        this.name = name;
        this.age = age;
        this.scores = new int[3];            // ⑦ new 로 만든 배열
        count++;
    }

    public int sum() {
        int total = 0;                       // ⑧ 지역변수
        for (int s : scores) total += s;
        return total;
    }

    public static void main(String[] args) {
        Person p = new Person("홍길동", 20);  // ⑨ 참조변수 p / ⑩ Person 객체
        p.scores[0] = 90;
    }
}
```

### 메서드 영역 할당
- NATION
- count
- 메서드 코드인 sum, main
- 클래스

### 힙 영역 할당
- 인스턴스 변수인 name, age, scores
- new로 만든 배열과 객체 (int[], Person)
- p의 실제 값

### 스택 영역 할당 (프레임 단위)
- 생성자 매개변수인 name, age
- 지역변수인 total
- p (Person의 주소값만)

### Call by Value

```java
public class Main {
	public static void main(String[] args) {
		int var = 1;
		int[] arr = {1};
				
		add_value(var); // 변수 자체를 보냄
		System.out.println(var); // 1: 값 변화가 없음
		
		add_reference(arr); // 배열 자체를 보냄
		System.out.println(arr[0]); // 101 : 값 변화
		
	}
	static void add_value(int var_arg) {
		var_arg += 100;
	}
	static void add_reference(int[] arr_arg) {
		arr_arg[0] += 100;
	}
}
```

#### add_value에서 var값이 변화가 없는 이유?

1. main()의 스택 프레임에 var = 1로 저장
2. add_value()의 개별 스택 프레임에 새로운 변수의 var_arg에 +100이 
3. var와 var_arg는 다른 스택 프레임이므로 var값에는 아무런 변화가 없음

#### add_reference에서는 arr 배열값이 같은 이유?

1. main()의 힙 영역에 arr 주소값 저장
2. 스택 영역에서 arr의 주소값 참조
3. add_reference() 스택 프레임에서 같은 배열 arr의 주소값을 참조한 상태로 생성
4. arr_arg 배열과 arr 배열은 같은 힙의 주소값을 바라보고 있기 때문에 값의 변형이 일어남

arr_arg는 arr의 주소를 가진 새로운 변수를 선언한 것이므로 단순히 주소값의 복사
결론적으로 call by value로만 작동하며, 원시값이 복사되느냐 주소값이 복사되느냐의 차이만 존재

#### 문제 예시
```java
다음 코드 실행 후 `arr[0]`과 `num`의 값은?

static void change(int[] arr, int num) {
    arr[0] = 100;
    num = 100;
}
public static void main(String[] args) {
    int[] arr = {1, 2, 3};
    int num = 1;
    change(arr, num); // 100,1
}
```

메서드 단위로 프레임의 주소값이 어떻게 참조되어있는지를 알아내는 것이 중요

## 싱글톤

> 프로그램 전체에서 단 하나의 객체만 만들도록 해야할 때

- 클래스 외부에서 new 연산자로 생성자를 호출할 수 없도록 막아야함
- private 접근 제한자로 제한
- 외부에서는 정적 메소드인 getInstance()를 선언 lol하고 호출할 수 있도록 함

```java
public class Singleton {
	// 정적 필드
	private static Singleton singleton = new Singleton();
	
	// 생성자
	private Singleton() {}
	
	// 정적 메서드
	static Singleton getSingleton() {
		return singleton;
	}
}
```

## 상속

### 초기화 순서

1. 부모 static 초기화
2. 자식 static 초기화 
3. 부모 인스턴스 블록
4. 부모 생성자
5. 자식 인스턴스 블록
6. 자식 생성자

### 생성자 상속

```java
class Parent {
    Parent(int x) { }          // 매개변수 있는 생성자만 정의
}
class Child extends Parent {
    Child() { }                // ❌ 컴파일 에러!
}
```

- 부모 클래스에서 기본 생성자가 아닌 임의로 생성자를 만들면, 자식 클래스에서 super() 호출을 자동으로 안함
- 이를 해결하려면 자식 클래스에서 super(0) 을 호출해줘야함

#### 문제 예시

```java
class Parent {
    static { System.out.print("A"); }
    { System.out.print("B"); }
    Parent() { System.out.print("C"); }
}
class Child extends Parent {
    static { System.out.print("D"); }
    { System.out.print("E"); }
    Child() { System.out.print("F"); }
}
// new Child(); 실행
```

호출 순서 : ADBCEF

static 메서드 안에서 **this, super, 인스턴스 멤버 사용 불가**

## 다형성 / 오버로딩 / 오버라이딩

### 오버로딩

> 같은 클래스 내 같은 이름, 다른 매개변수
> 개념적으로 같은 일인데, 입력 형태를 다르게 할 때 사용

- 메서드명 동일
- 매개변수 반드시 달라야함

### 오버라이딩

> 부모 메서드를 자식이 재정의

- 메서드명 동일
- 매개변수 완전히 동일
- 반환 타입 동일

### 다형성

```java
class A {
    String name = "A";
    String get() { return "A-get"; }
    static String sm() { return "A-static"; }
}
class B extends A {
    String name = "B";
    String get() { return "B-get"; }
    static String sm() { return "B-static"; }
}
A obj = new B();
System.out.println(obj.name + " " + obj.get() + " " + obj.sm());
```

1. **obj.name**
	- 필드는 오버라이딩되지 않음
	- obj의 타입이 A이므로 "A"
	- 캐스팅 하면 "B" 출력
2. **obj.get()**
	- 인스턴스 메서드만 동적 바인딩
	- 실제 객체의 재정의된 메서드를 호출
	- B-get 호출
	- 캐스팅해도 똑같이 실제 객체의 B-get 호출
	- 부모인 A를 부르려면 super.get()을 써야함
3. **obj.sm()**
	- 오버라이딩 되지 않고 참조변수 타입 기준으로 결정
	- A-static 호출

## 추상 클래스 / 인터페이스

### 추상 클래스 (abstract class / extends)

> 어떻게 구현할지?를 물려줄 때

- 필드, 상태를 물려줘야 하고, 공통 구현을 그대로 자식 클래스가 써야할 때 
- 부모와 자식의 강한 결합
- protected 메서드가 필요할 때

### 인터페이스 (interface / implements)

> 무엇을 하는가?만 같고, 어떻게 하는가?는 다를 때

- 공유할 구현은 없이 메서드명(하는 일)만 같을 때
- 구현체가 여러 개이고 앞으로 늘어날때
- 생성자를 정의할 수 없음

```java
interface Config {
    int MAX = 100;                          
}
```

- public static final를 붙이지 않아도, interface 에서는 자동으로 상수로 선언
	- 초기화 무조건 해줘야함

```java
class Impl implements Service {
    void run() { }           // ❌ 에러 — public 누락
    public void run() { }    // ✅
}
```

- 무조건 public 선언해줘야함

## 예외 처리 (checked / unchecked)

### checked Exception

- 컴파일 시 예외 처리
- try-catch 문으로 예외를 강제적으로 처리하지 않으면 컴파일 안됨
- IOException, SQLException
- 트랜잭션 롤백 시에 checked는 그대로 커밋해서 안씀
- 강제적으로 재시도나 복구 로직을 무조건 해야하는 예외상황일때 사용

### unchecked Exception

- 실행 시 예외 처리
- 복구를 하지 못하고 그냥 오류 응답을 보낼 때 주로 사용

## 컬렉션

### HashSet / LinkedHashSet / TreeSet

순서 : 업음 / 삽입 순서 / 정렬 순서
중복 판정 : hashCode() 비교 후 같으면 equals() 비교 / compareTo 로 비교

## 제네릭

### PECS

```java
List<? extends Number> reader;   // 읽기만 가능, add 불가 (null만 예외)
List<? super Integer> writer;    // 쓰기 가능, 읽으면 Object로만
```

- extends에는 add가 안됨
	- List에 어떤 타입이 올지 모르기 때문

#### 대입 가능 범위

```java
List<? extends Number> a;
a = new ArrayList<Integer>();   // ✅ Integer는 Number 하위
a = new ArrayList<Double>();    // ✅
a = new ArrayList<Object>();    // ❌ Object는 Number 상위

List<? super Integer> b;
b = new ArrayList<Integer>();   // ✅
b = new ArrayList<Number>();    // ✅ Number는 Integer 상위
b = new ArrayList<Object>();    // ✅ Object도 상위
b = new ArrayList<Double>();    // ❌ Double은 Integer의 상위가 아님
```

#### 제네릭 메서드는 반환타입 앞에 위치

```java
class Box<T> { }                          // 클래스 제네릭
<T> void print(T t) { }                   // 메서드 제네릭 — 반환타입 앞에 위치
<T extends Number> void sum(T t) { }      // 제한된 타입 파라미터
```

## 람다와 스트림

### 문법

```java
(매개변수) -> { 실행문; }
(a, b) -> a + b        // 매개변수 타입 생략 가능
a -> a * 2             // 매개변수 1개면 괄호 생략 가능
() -> System.out.println("hi")  // 매개변수 없으면 () 필수
(a, b) -> { return a + b; }     // 중괄호 쓰면 return 명시
```

## 멀티 스레드

|상태|진입 조건|
|---|---|
|NEW|객체 생성 직후, start() 전|
|RUNNABLE|start() 호출 후|
|BLOCKED|**synchronized 락 대기 중**|
|WAITING|wait(), join() — **시간 제한 없이** 대기|
|TIMED_WAITING|sleep(ms), wait(ms), join(ms)|
|TERMINATED|run() 종료|
- sleep()은 락 반납 안하고, wait()은 반납함
- sleep()은 interrupt로 깨어나고, wait()은 notify()로 깨어남

## 실습 대비

### 입출력

```java
import java.util.*;
import java.io.*;

BufferedReader bf = new BufferedReader(new InputStreamReader(System.in));
```

### 단축키 정리

자동 생성 : sources 탭
ctrl + space : 자동완성
ctrl + 1 : 에러 줄에서 해결책 제시

### 구현체 impl 싱글톤 코드 구현

```java
public class BookManagerImpl implements BookManager {
	private static BookManager instance = new BookManagerImpl();
	public static BookManager getInstance() {
		return instance;
	}
}
```

### 구현체 구현시 주의사항

#### 1. 자식 클래스 생성자 생성 시 인자 추가 인지

#### 2. getSeries() 캐스팅 주의

```java
List<Series> temp = new ArrayList<>();
for (Movie movie : movies) {
    if (movie instanceof Series) temp.add((Series) movie);
}
return temp.toArray(new Series[temp.size()]);
```
- Series 타입으로 리스트 생성

#### 3. 제목 조회 시 contains()로 조회
- equals() 가 아닌 contains() 사용

#### 4. getMovies()에서 Movie[] 반환 시 주의

```java
for(Movie movie : movies) {
	if(movie.getClass().equals(Movie.class)) {
		temp.add(movie);
	}
}
```

- instance of 대신 **.getClass().equals(Movie.class)** 사용